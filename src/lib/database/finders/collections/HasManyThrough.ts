import { Collection } from "../Collection"
import { Model } from "../../Model"
import { } from "../../DatabaseConnection"

export class HasManyThrough<M, T extends Model<M>> extends Collection<M, T> {

}